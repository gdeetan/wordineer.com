# Russian Names 1500 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Expand the Russian name dataset to at least 1500 listed name records and update the page title for the larger dataset.

**Architecture:** Preserve the current JSON contract: `given` records keep patronymic fields, and `surnames` records keep male/female Latin and Cyrillic variants. Update the live deploy page and template source metadata so the title claim matches the data.

**Tech Stack:** Static HTML, JSON, Node.js for validation.

---

### Task 1: Expand Dataset

**Files:**
- Modify: `wordineer-deploy/data/russian-names.json`

- [ ] Add enough unique surname records to bring `given.length + surnames.length` to at least 1500.
- [ ] Preserve existing given-name records unchanged.
- [ ] Validate JSON and confirm total count.

### Task 2: Update Page Metadata

**Files:**
- Modify: `wordineer-deploy/random-russian-name-generator.html`
- Modify: `template-deploy/tools-src/random-russian-name-generator.html`

- [ ] Update `<title>` to a CTR-focused `1500+ Russian Names` title.
- [ ] Update matching Open Graph title text.
- [ ] Confirm page still references `russian-names.json`.
